# Allowlist versionada de warnings de build (SPEC 01).
# Cada entrada requiere: pattern (exacto), tool, justification, responsible,
# review_date. Cualquier warning no listado produce FAIL en el build de
# aceptación. Se completa durante el Paso 5 con los warnings observados.
allowlist: []
